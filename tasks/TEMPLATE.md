# <Topic>

<!--
Canonical skeleton for a task log. Copy to tasks/<topic>.md and fill it in; the
convention it follows is docs/framework-maintenance.md "Task logs".

A task log exists so a *later session* can resume work without re-deriving what
was already learned. That means recording the reasoning and the dead ends, not
just the outcome. Keep the Status line current, and append to the progress log
as work lands rather than rewriting history.
-->

**Status:** OPEN | IN PROGRESS | RESOLVED — last updated YYYY-MM-DD

## Problem

What is wrong / what we want, and why it matters. Include how it reproduces if
it is a defect: the deck or input, rank count, backend, system, and the
observable symptom.

## Approach

The strategy, the phases, and the key decisions and constraints. Record
hypotheses that were **ruled out** here as well, so a future session does not
re-test them.

## Progress log

<!-- Newest entries at the end. One entry per work session or experiment:
     what was done, what was learned, what is next. -->

- YYYY-MM-DD — <what was done, what was learned, what's next>
